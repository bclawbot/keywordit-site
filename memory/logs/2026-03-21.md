# Session Log — 2026-03-21

## Session — 13:00

### Decisions Made
- Added `KeepAlive { SuccessfulExit: false }` to `ai.openclaw.heartbeat.plist` so launchd auto-restarts heartbeat after SIGTERM/crash kills (won't restart on clean exit 0).
- Added auto-restart via `launchctl kickstart -k user/501/ai.openclaw.heartbeat` to `watchdog.py:check_heartbeat_staleness()` — fires alongside the Telegram alert when log is stale and heartbeat isn't running.
- Increased `validation.py` timeout: 900s → 1800s in both `heartbeat.py` and `watchdog.py` STAGE_TIMEOUTS. Last run had 3,699 opps and timed out at 900s.
- Created `deploy_dashboard.sh` — Stage 6 in the pipeline. Pushes `dashboard.html` as `index.html` to `gh-pages` branch via git worktree (no branch switching in main tree). Gracefully exits 0 if no remote configured (safe to run before GitHub setup).
- Added `deploy_dashboard.sh` as Stage 6 to `heartbeat.py` STAGES list with 120s timeout.
- Added bash-script support to `_exec_stage()` in `heartbeat.py`: detects `.sh` extension and runs with `bash` instead of `python3`.

### Files Changed
- `~/Library/LaunchAgents/ai.openclaw.heartbeat.plist` — added KeepAlive block (line 17)
- `~/.openclaw/workspace/watchdog.py` — auto-restart kickstart in `check_heartbeat_staleness()` (~line 221); validation.py timeout 900→1800 in STAGE_TIMEOUTS
- `~/.openclaw/workspace/heartbeat.py` — Stage 6 added to STAGES; validation.py timeout 900→1800; bash script support in `_exec_stage()`; `deploy_dashboard.sh` added to STAGE_TIMEOUTS
- `~/.openclaw/workspace/deploy_dashboard.sh` — new file, created and made executable

### Constraints Discovered
- `validation.py` longevity cache was already optimized (module-level `_history_cache`). The timeout was the only issue, not per-record file scanning.
- `deploy_dashboard.sh` requires manual GitHub one-time setup before it will actually push: `git remote add origin`, push main, create orphan gh-pages branch. Script silently exits 0 until then.
- Stage 6 is fatal in the sense that failure is counted in `errors`, but non-blocking (pipeline continues). Graceful no-op when no remote exists.

### Open Questions
- GitHub repo not yet created. User needs to run the one-time setup commands from the plan to connect workspace to a GitHub remote.
- Heartbeat started running immediately on plist reload (PID 98704) — first run with Stage 6 will skip deploy (no remote).
