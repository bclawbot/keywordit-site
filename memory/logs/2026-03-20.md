## Session — 17:17

### Task
Investigated heartbeat gap + zombie process, then built process watchdog.

### Findings
- `keyword_expander.py` (PID 33028) had been running since Tuesday (4+ days) — zombie from a stuck pipeline cycle. Killed manually.
- `heartbeat.py` runs hourly via launchd but lock file (`/tmp/openclaw_heartbeat.lock`) blocks new runs while a process is alive — zombie caused ~17h gap in heartbeat cycles (Mar 19 18:09 → Mar 20 11:34AM).
- Current heartbeat run started 11:34AM, currently executing `keyword_extractor.py` (127 min / 360 min limit) — healthy.
- No GitHub push in pipeline (all `.commit()` calls are SQLite, not git).

### Changes Made
- **Killed**: `keyword_expander.py` PID 33028 (zombie, 4+ days old)
- **Created**: `~/.openclaw/workspace/watchdog.py` — process health monitor
- **Created**: `~/Library/LaunchAgents/ai.openclaw.watchdog.plist` — runs watchdog every 5 min
- **Loaded**: `launchctl load ai.openclaw.watchdog.plist`

### Watchdog Coverage
| Check | Threshold | Action |
|-------|-----------|--------|
| heartbeat.log stale + not running | >2h | Telegram alert |
| Pipeline stage zombie | >2× stage timeout | Kill + alert |
| heartbeat.py stuck | >8h | Kill + clear lock + alert |
| Service dead (gateway/dwight/litellm) | not in launchctl | Alert only |
| Ollama / LiteLLM HTTP | unresponsive | Alert |

Alert dedup: re-alerts every 30 min max. State in `/tmp/watchdog_alerted.json`.

### Constraints Discovered
- heartbeat.log only updates after full cycle completes — stale log check must be suppressed while heartbeat.py is actively running (keyword_extractor alone can take 6h).
- LiteLLM `/v1/models` returns 401; use `/` (returns 200) for health check.

### Open Questions
- Should watchdog also restart the heartbeat launchd service if it's found dead (not just alert)?

## Session — 20:xx

### Task
Fixed dashboard showing repeated TREND values (same source_trend label appearing 161–288 times per trending article).

### Root Cause
`dashboard_builder.py` loads full `validation_history.jsonl` (15,844 records → 4,493 after keyword|country dedup). One viral article seeds hundreds of keyword-country combinations, all sharing the same `source_trend` field — making the TREND column look like solid repeats.

### Fix
`dashboard_builder.py` — added per-source_trend cap block (after enrichment loop, before metadata block):
- Group by `source_trend` (non-empty only)
- Sort each group by `opportunity_score` desc, keep top 15
- Entries with empty `source_trend` kept in full
- Re-sort globally by `opportunity_score` for consistent `#` ranking

### Result
- 4,493 → 2,597 rows (−42%)
- 6.8MB → 3.8MB dashboard
- Every trend capped at exactly 15 entries; no changes to template

## Session — 17:30

### Task
Pipeline speed optimization — reduce run time without removing capabilities.

### Changes Made
1. `keyword_extractor.py:109` — `DFS_ASYNC_POLL_INTERVAL = 30` → `10` (3× faster DataForSEO polling)
2. `vetting.py:356–357` — `asyncio.Semaphore(5)` → `Semaphore(15)`, `ThreadPoolExecutor(5)` → `(15)` (3× more concurrent SERP checks)
3. `heartbeat.py` — added `PARALLEL_WITH = {"keyword_expander.py"}`, `_exec_stage()` helper, parallel execution via `concurrent.futures.ThreadPoolExecutor` — keyword_expander (~10 min) now overlaps with keyword_extractor (2h+)

### Expected Savings
- DFS polling: 20–40 min (responds when tasks ready instead of fixed 30s sleeps)
- Parallel stages 2a+2: ~10 min
- vetting concurrency: ~10 min
- Total: 40–60 min per pipeline run

### Constraints
- LLM calls remain sequential (Ollama is single-threaded) — not changed
- `errors` list shared between parallel threads is safe in CPython (GIL)

## Session — Experimental Pipeline Foundation (Session 1 of 3)

### Task
Implemented Phases 1-4 of the Experimental Dashboard / Pipeline from DWIGHT_SESSION1_FOUNDATION.md.

### Files Created
- `data/hard_filters.json` — country whitelist, blocklist words/verticals, Tier C rules
- `data/winner_dna.json` — 60 entries: top 50 real revenue keywords from CSV + 10 anti-patterns (fields: keyword, country, revenue, clicks, rpc, vertical, entity, entity_type, intent_signals, demographic, linguistic_flags, why_it_works/why_it_fails)
- `data/entity_registry.json` — 13 entity types (membership_retailer, insurance_carrier, government_program, demographic_org, gig_platform, employer, destination, medical_condition, pharma_drug, vehicle_type, travel_brand, auction_program, education_format) with proven/test/blocked per country
- `data/vertical_cpc_reference.json` — 13 verticals with country×vertical multiplier matrix, intent_modifiers, scoring_formula (v2.0)
- `modules/__init__.py` — empty init
- `modules/hard_filter.py` — pre-LLM noise gate, returns (bool, REASON_CODE). 100% pass on 50 winner keywords, 100% block on 10 anti-patterns
- `modules/linguistic_scorer.py` — regex-based structural signal detection (first_person_desperation, negative_qualifier, price_anchor, urgency_frame). Compound 1.2x bonus at signal_count >= 2, cap 2.5x
- `modules/keyword_scorer.py` — LLM few-shot scoring module. Loads winner_dna.json at runtime (15 positive + 10 negative). Output: {keyword, score 1-10, vertical_match, intent_signals, reasoning}. Falls back gracefully if LiteLLM unavailable

### Test Results
- Phase 1: All 4 JSON files parse cleanly
- Phase 2: 5/5 hard_filter spec tests pass. 50/50 winners PASS, 10/10 anti-patterns BLOCKED
- Phase 3: 3/3 linguistic_scorer spec tests pass (exact signal detection + bonus range)
- Phase 4: Prompt builds with correct sections (9,159 chars). LLM call untested (LiteLLM not started)
- golden_opportunities.json 10.9% pass rate — expected: 128/147 are GB keywords not in new whitelist. This is correct filtering behavior.

### Decisions
- winner_dna.json built from real CSV data (keywords_report_03-16.03.06.csv) via Python script, not hardcoded
- bad_credit added to negative_qualifier regex (was missing from spec, needed for test case 2)
- keyword_scorer.py is standalone new module (not modifying keyword_extractor.py) — pipeline integration is Session 2 scope
- GB not added to country_whitelist despite being in golden_opportunities.json — GB keywords avg $X/kw from spec data, consistent with Tier C exclusion

### Open Questions
- Session 2 scope: template_decomposer.py, template_expander.py, plausibility_checker.py, cpc_router.py
- LLM score test (Phase 4 spec requires live LLM run) — deferring to Session 2 integration test
- GB: the spec does NOT list GB in country_whitelist or Tier A/B/S. Current pipeline accepts it via country_config.py. Need decision on whether to add GB to whitelist for new pipeline.

## Session — Experimental Pipeline Session 2 (Phases 5–8)

### Task
Implemented Phases 5–8 of DWIGHT_SESSION2_PIPELINE.md: template expansion pipeline + pipeline integration.

### Files Created
- `modules/template_decomposer.py` — LLM batch decomposition. Exposes `decompose_batch(keywords, country)`. Calls LiteLLM first, falls back to direct Ollama. Strips `<think>` tags from qwen3 output. 3-attempt retry with backoff. Returns non-expandable fallbacks on failure.
- `modules/template_expander.py` — Pure Python entity-swap expansion. Exposes `expand_batch(decomposed, registry, country, quality_scores)`. Implements EXPANSION_BUDGET: 8 per PRIORITY (score≥14), 4 per PASS (score≥4), 500 per cycle, 25 per entity. Passes `vertical`/`vertical_match` through to expansions.
- `modules/plausibility_checker.py` — LLM plausibility filter. Exposes `check_batch(expansions)`. Adds `plausible: bool` to each expansion. Fail-open on LLM failure (preserves recall).
- `modules/cpc_router.py` — Two-track CPC routing. Exposes `route_for_validation(keywords, registry, vertical_ref)`. Track A: organic, government_program, PRIORITY+proven. Track B: all others. Track B gets `inherited_cpc` = `base_rpc × brand_mult` (no double-counting with country_tier). Hard cap 150 Track A per cycle.

### Files Modified
- `keyword_extractor.py` — Two surgical insertions:
  1. After normalization (line ~895): Steps 1+2 — hard_filter writes rejections to `data/filtered_out.log`, linguistic_scorer adds `linguistic_score` to each keyword dict
  2. Before OUTPUT.write_text (~line 1093): Steps 3–6 — decompose PRIORITY keywords, expand, plausibility check, entity discovery → `data/discovered_entities.jsonl`, CPC routing, merge Track B experimental into `passed`, write `data/expansion_results.jsonl` + `data/expansion_log.jsonl`
  - Both blocks wrapped in try/except — organic pipeline unaffected on failure

### Bug Fixed
- template_expander.py wasn't passing `vertical`/`vertical_match` through to expansion dicts → cpc_router fell back to "general" avg_rpc ($1.00) instead of auto_insurance ($3.74). Fixed by copying `decomp["vertical"]` into each expansion.

### Test Results
- hard_filter: "laser cutting machine price"/IN → BLOCKLIST_WORD (correct)
- linguistic_scorer: fires correctly on test keywords
- expand_batch: 3 PRIORITY keywords → 24 raw expansions
- cpc_router: Section8 → Track A, BJ's/AAA → Track B at $3.74e (correct)
- Merge: 3 organic + 5 experimental = 8 total output keywords
- keyword_extractor.py syntax: clean (py_compile passes)
- LLM calls (decomposer/plausibility) NOT tested live — require running LiteLLM

### Data Files Artifacts (created at runtime, not committed)
- `data/filtered_out.log` — keywords rejected by hard_filter with reason codes
- `data/expansion_results.jsonl` — one JSONL line per plausible expansion
- `data/discovered_entities.jsonl` — entities found by decomposer not in registry
- `data/expansion_log.jsonl` — one entry per pipeline run with cycle_stats

### Open Questions
- Session 3 scope: Experimental tab + Performance tab in dashboard_builder.py, csv_importer.py weekly feedback loop
- LLM integration test deferred — needs `heartbeat.py` dry-run with a real small batch
- `data/` directory files should be added to .gitignore or created as empty stubs

## Session — Session 3 (Dwight Experimental — Phases 9–12)

### Task
Complete Session 3 of Dwight Experimental pipeline: dashboard tabs, csv importer, Telegram alerts.

### Files Changed
- `dashboard_builder.py` — Phase 9/10: Added `_build_experimental_tab()`, `_build_performance_tab()`, `EXTRA_CSS`, `TAB_NAV_HTML`, updated placeholder injection to 5 points. Bug fix: skip non-dict top-level keys in entity_registry (version/last_updated).
- `dashboard_template.html` — Added 3 injection placeholders: `__EXTRA_CSS__`, `__TAB_NAV__`, `__EXTRA_TABS__`.
- `scripts/csv_importer.py` — NEW: Phase 11 weekly feedback loop. Parses KeywordIt CSV, matches expansion_results.jsonl, computes hit rates, flags promotion candidates, updates entity_registry.json (flags only), updates vertical_cpc_reference.json (rolling 70/30), maintains winner_dna.json (50 winners + 10 anti-patterns), detects pipeline drift, writes performance_cache.json.
- `telegram_bot.py` — Phase 12: Added `send_experimental_alerts()` standalone function with 3 alert types: NEW_ENTITY_DISCOVERED, ENTITY_PROMOTION_CANDIDATE, PIPELINE_DRIFT. Uses .telegram_sent_entities.json to deduplicate entity alerts.
- `heartbeat.py` — Wired `send_experimental_alerts()` call after dashboard notification block.

### Constraints Discovered
- entity_registry.json has top-level `version` and `last_updated` string keys — must skip non-dict values when iterating entity types.
- winner_dna.json is a flat list (not dict with 'winners'/'anti_patterns' keys), 60 entries total (50 winners + 10 anti-patterns), distinguished by `why_it_works` vs `why_it_fails` field presence.
- Telegram alerts use raw urllib (not PTB async) so they can be called from heartbeat.py import.

### Open Questions
- csv_importer.py has never been tested with a real CSV — first real test will be next KeywordIt export.
- Performance tab will show "awaiting import" until first csv_importer.py run completes.
